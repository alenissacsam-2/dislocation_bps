# secrets — never commit
.env
.env.*
!.env.example
*.key
keypair*.json
wallet*.json
secrets/

# rust
target/
**/*.rs.bk

# node
node_modules/
.next/
dist/

# ...but the dashboard is not build output. It is a hand-written single file with no
# build step, and the rule above was silently the only thing standing between it and
# being lost — it was never tracked at all.
!dashboard/dist/
!dashboard/dist/index.html
build/
*.tsbuildinfo

# python
__pycache__/
*.pyc
.venv/
venv/

# data
*.db
*.db-wal
*.db-shm
data/
logs/
*.parquet

# os / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
config.toml
cryptobot*.db
cryptobot*.db-wal
cryptobot*.db-shm

# Tauri codegen
crates/desk/gen/


# bot log, written by cryptobot-desk
cb-bot.log


# Sidecar staged by scripts/installer.ps1 — build output, not source.
crates/desk/binaries/

# An Apple configuration profile carrying an inference-gateway API key. Personal,
# unrelated to this project, and swept in once by a `git add -A`. Never push it.
Claude.mobileconfig
*.mobileconfig

# Retired artifacts moved aside on 2026-09-23: old ledgers, stale builds. Safe to delete.
_retired/
token-demand.json
token-demand.json.tmp
lookup-table.json
